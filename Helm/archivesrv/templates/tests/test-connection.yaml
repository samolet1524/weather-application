apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "archivesrv.fullname" . }}-test-connection"
  labels:
    {{- include "archivesrv.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "archivesrv.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
