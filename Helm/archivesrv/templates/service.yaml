apiVersion: v1
kind: Service
metadata:
  name: {{ include "archivesrv.fullname" . }}
  labels:
    {{- include "archivesrv.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "archivesrv.selectorLabels" . | nindent 4 }}
