apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "modulechart.fullname" . }}-test-connection"
  labels:
    {{- include "modulechart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "modulechart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
