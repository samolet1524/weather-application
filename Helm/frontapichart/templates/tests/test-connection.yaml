apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "frontapichart.fullname" . }}-test-connection"
  labels:
    {{- include "frontapichart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "frontapichart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
