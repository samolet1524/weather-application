apiVersion: v1
kind: Service
metadata:
  name: {{ include "frontapichart.fullname" . }}
  labels:
    {{- include "frontapichart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "frontapichart.selectorLabels" . | nindent 4 }}
